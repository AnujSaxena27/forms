# ==========================================
# MONGODB CONFIGURATION
# ==========================================
# REQUIRED: MongoDB Atlas connection string
# Format: mongodb+srv://username:password@cluster.mongodb.net/database
# Get this from: https://cloud.mongodb.com
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database

# ==========================================
# CLOUDINARY CONFIGURATION
# ==========================================
# REQUIRED: Cloudinary credentials for file uploads
# Get these from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Cloudinary folder prefix (where files will be stored)
# Example: "myapp" will create folders like "myapp/applications/photographs"
CLOUDINARY_FOLDER_PREFIX=your_folder_prefix

# ==========================================
# APPLICATION CONFIGURATION
# ==========================================
# Application URL (used for CORS and origin validation)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ==========================================
# ENVIRONMENT
# ==========================================
# Options: development, production, test
NODE_ENV=development

# ==========================================
# OPTIONAL: SECURITY CONFIGURATIONS
# ==========================================
# Maximum file upload sizes (in bytes)
# MAX_IMAGE_SIZE=5242880  # 5MB
# MAX_PDF_SIZE=10485760   # 10MB

# Rate limiting (requests per minute)
# RATE_LIMIT_MAX_REQUESTS=100
# RATE_LIMIT_WINDOW_MS=60000

# ==========================================
# SETUP INSTRUCTIONS
# ==========================================
#
# 1. MONGODB ATLAS SETUP:
#    - Sign up at https://cloud.mongodb.com
#    - Create a new cluster (free tier available)
#    - Go to Database Access → Add New Database User
#    - Go to Network Access → Add IP Address (0.0.0.0/0 for all IPs or your specific IP)
#    - Go to Database → Connect → Connect your application
#    - Copy the connection string and replace <password> with your database user password
#
# 2. CLOUDINARY SETUP:
#    - Sign up at https://cloudinary.com (free tier available)
#    - Go to Dashboard → Account Details
#    - Copy Cloud Name, API Key, and API Secret
#
# 3. COPY THIS FILE:
#    - Copy this file to .env in the root directory
#    - Replace all placeholder values with your actual credentials
#    - Never commit .env to version control (it's in .gitignore)
#
# 4. VERIFY SETUP:
#    - Run: npm run dev
#    - Check terminal for connection status
#    - Test file upload functionality
#
# ==========================================
# SECURITY NOTES
# ==========================================
#
# ⚠️  NEVER commit .env file to Git
# ⚠️  Keep credentials secure and rotate regularly
# ⚠️  Use different credentials for dev/staging/production
# ⚠️  Enable 2FA on MongoDB Atlas and Cloudinary accounts
# ⚠️  Monitor API usage and set up billing alerts
#
# For Vercel deployment:
# - Add all environment variables in Vercel Dashboard
# - Project Settings → Environment Variables
# - Add separately for Production, Preview, and Development
#
# ==========================================
